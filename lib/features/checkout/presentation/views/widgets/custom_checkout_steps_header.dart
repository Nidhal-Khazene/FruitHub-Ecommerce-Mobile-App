import 'package:flutter/material.dart';

class CustomCheckoutHeader extends StatelessWidget {
  const CustomCheckoutHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
