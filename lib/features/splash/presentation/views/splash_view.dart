import 'package:flutter/material.dart';

class SplashView extends StatelessWidget {
  const SplashView({super.key});
  static const splashViewRoute = "/SplashView";
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
